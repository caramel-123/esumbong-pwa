# Copy to .env and adjust if not using the provided docker-compose Postgres.
# Local Docker Postgres (docker compose up -d):
DATABASE_URL="postgresql://esumbong:esumbong@localhost:5432/esumbong"

# Or point this at a hosted free-tier Postgres (Neon, Supabase, Railway, etc.)
# if you'd rather skip local Docker setup during the hackathon.
